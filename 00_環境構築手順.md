# ゼロからの環境構築手順

Eclipse（Pleiades）、Oracle Databaseともに未インストールのPC（Windows 10/11）を前提とした、
最初から動作確認までの手順です。所要時間の目安は合計1.5〜2.5時間程度です
（Oracleのインストール・ダウンロードに時間がかかります）。

対象のフォルダ構成・ソースは、先に共有した `order-system.zip` を展開したものを使用します。

---

## 全体の流れ

1. Eclipse（Pleiades All in One）のインストール
2. Oracle Database 21c Express Edition（XE）のインストール
3. Oracle側の初期設定（アプリ用ユーザー作成）
4. テーブル作成・サンプルデータ投入
5. サンプルシステムのプロジェクト設定（db.properties書き換え）
6. EclipseへMavenプロジェクトとしてインポート
7. Tomcatサーバーの登録・起動確認
8. 画面の動作確認
9. マスタ管理コマンドの動作確認
10. よくあるエラーと対処

---

## 1. Eclipse（Pleiades All in One）のインストール

Pleiadesは「Eclipse本体＋日本語化＋JDK＋Tomcat」がまとめて入っているパッケージなので、
基本的にはこれ1つでJDKやTomcatを別途用意する必要はありません。

1. ブラウザで以下のサイトにアクセスする。
   `https://willbrains.jp/`
2. トップページのバージョン一覧から、最新の Eclipse バージョンをクリックする
   （2026年時点では Eclipse 2024 以降の比較的新しいバージョンで問題ありません）。
3. 遷移先のダウンロードページで「Java」の「Full Edition」を選択する。
   - OSは Windows、64bit を選択してください。
4. ダウンロードした `pleiades-....exe`（または `.zip`）を実行し、インストール先フォルダを指定する。
   - 例： `C:\pleiades\` （パスに日本語・スペースを含めないこと）
5. インストール完了後、`C:\pleiades\eclipse\eclipse.exe` をダブルクリックしてEclipseを起動する。
   - 初回起動時に「ワークスペースの選択」画面が出るので、任意のフォルダ（例：`C:\workspace`）を指定する。
6. Eclipseが起動し、メニューや画面がすべて日本語になっていれば成功。

### JDKのバージョンについて
Pleiades Full Editionには複数バージョンのJDK（Java 8/11/17/21など）が同梱されています。
本サンプルは Java 11 相当でビルドする設定にしていますので、特別な作業は不要ですが、
もしビルドエラーが出た場合は以下を確認してください。

- Eclipseメニュー「ウィンドウ」→「設定」→「Java」→「インストール済みのJRE」で、
  JDK 11以降が一覧にあることを確認する（Pleiadesには複数バージョンが同梱済み）。

---

## 2. Oracle Database 21c Express Edition（XE）のインストール

### (1) ダウンロード
1. ブラウザで以下にアクセスする。
   `https://www.oracle.com/jp/database/technologies/xe-downloads.html`
2. 「Oracle Database 21c Express Edition for Windows x64」をクリックする。
   - ダウンロードには無料のOracleアカウントが必要な場合があります。持っていない場合は
     案内に従ってアカウントを作成してください。
3. `OracleXE213_Win64.zip`（バージョンにより数字は前後します）がダウンロードされる。

### (2) インストール
1. ダウンロードしたzipを、パスに日本語・スペースを含まないフォルダに展開する。
2. 展開したフォルダ内の `setup.exe` を右クリック →「管理者として実行」する。
   - 事前に環境変数 `ORACLE_HOME` や `TNS_ADMIN` が設定されている場合は解除しておく。
   - Windowsに管理者グループのユーザーでログインしていることを確認する。
3. インストーラーの案内に従って「次へ」を押していく。
   - 使用許諾条項に同意する。
   - インストール先フォルダはデフォルトのままでよい（例：`C:\oracle\product\21c\`）。
   - **データベース・パスワード**の入力を求められるので、分かりやすいパスワードを設定する
     （このパスワードが `SYS` / `SYSTEM` / `PDBADMIN` 共通のパスワードになります。忘れないようにメモしておく）。
4. インストール完了後の画面に接続文字列（`localhost:1521/XEPDB1` など）が表示されるのでメモしておく。

### (3) インストール後の確認
コマンドプロンプトを開き、以下を実行してSQL*Plusで接続できるか確認する。

```bat
sqlplus system/設定したパスワード@localhost:1521/XEPDB1
```

`SQL>` プロンプトが表示されれば成功です。`exit` で抜けられます。

> ポイント： Oracle 21c以降はマルチテナント構成のため、通常の業務データは
> ルート（CDB）ではなく **プラガブル・データベース `XEPDB1`** に接続して操作します。
> 本手順・サンプルの `db.properties` もすべて `XEPDB1` 宛てに設定します。

---

## 3. Oracle側の初期設定（アプリ用ユーザー作成）

`SYSTEM`ユーザーで受発注管理システム専用のユーザー（スキーマ）を作成します。

1. コマンドプロンプトで以下を実行してSQL*Plusに接続する。

```bat
sqlplus system/設定したパスワード@localhost:1521/XEPDB1
```

2. 以下のSQLを1行ずつ実行し、アプリ専用ユーザー `order_app` を作成する。

```sql
CREATE USER order_app IDENTIFIED BY order_app_pass;
GRANT CONNECT, RESOURCE TO order_app;
GRANT UNLIMITED TABLESPACE TO order_app;
```

- `order_app_pass` の部分は任意のパスワードに変更してかまいませんが、
  後で `db.properties` に設定する値と一致させてください。
- パスワードポリシーでエラーになる場合は、より複雑なパスワード
  （英大文字・小文字・数字を含む8文字以上など）に変更してください。

3. `exit` でSQL*Plusを終了する。

---

## 4. テーブル作成・サンプルデータ投入

`order-system.zip` を展開したフォルダの `sql` フォルダ内のスクリプトを、
先ほど作成した `order_app` ユーザーで実行します。

1. コマンドプロンプトで以下を実行し、`order_app` ユーザーで接続する。

```bat
sqlplus order_app/order_app_pass@localhost:1521/XEPDB1
```

2. テーブル作成スクリプトを実行する（初回はDROP文でエラーが出ますが無視してください）。

```sql
@C:\path\to\order-system\sql\01_create_tables.sql
```

3. サンプルデータ投入スクリプトを実行する。

```sql
@C:\path\to\order-system\sql\02_sample_data.sql
```

4. 以下のSQLでデータが入っていることを確認する。

```sql
SELECT * FROM MODEL_MASTER;
SELECT * FROM ORDER_MANAGEMENT;
```

5. `exit` で終了する。

---

## 5. サンプルシステムのプロジェクト設定

`order-system` フォルダ内の `src/main/resources/db.properties` を、
テキストエディタで以下のように書き換える（`XEPDB1` 形式の接続文字列に注意）。

```properties
db.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
db.user=order_app
db.password=order_app_pass
```

> `:xe`（SID形式）ではなく `/XEPDB1`（サービス名形式）である点に注意してください。
> 21c以降のOracle XEでは、通常こちらの形式で接続します。

---

## 6. EclipseへMavenプロジェクトとしてインポート

1. Eclipseを起動し、メニュー「ファイル」→「インポート」を選択する。
2. 「Maven」→「既存のMavenプロジェクト」を選択して「次へ」。
3. 「ルート・ディレクトリー」で、展開した `order-system` フォルダを指定する。
4. `pom.xml` が検出されるのでチェックを入れて「完了」。
5. しばらく待つと、Mavenが依存ライブラリ（JSTL、Oracle JDBCドライバ`ojdbc8`等）を
   自動的にダウンロードし、プロジェクトがビルドされる。
   - 「プロジェクト・エクスプローラー」にエラーマーク（赤い×）が出ていないか確認する。

### 依存ライブラリが取得できない場合（社内プロキシ等でブロックされる場合）
1. Oracle公式サイトから `ojdbc8.jar` を手動でダウンロードする。
   `https://www.oracle.com/database/technologies/appdev/jdbc-downloads.html`
2. プロジェクトを右クリック →「プロパティ」→「Javaのビルド・パス」→「ライブラリー」タブ
   →「外部JARの追加」で、ダウンロードした `ojdbc8.jar` を追加する。
3. あわせて `order-system\lib` フォルダにも同じ `ojdbc8.jar` をコピーしておく
   （後述のマスタ管理コマンド実行時に使用するため）。

---

## 7. Tomcatサーバーの登録・起動確認

Pleiadesには複数バージョンのTomcatが同梱されています（インストールフォルダ配下の
`tomcat` フォルダを参照）。

1. Eclipse下部の「サーバー」タブを開く（表示されていない場合は「ウィンドウ」→
   「ビューの表示」→「その他」→「サーバー」→「サーバー」）。
2. 「サーバーがありません。ここをクリックして新規に作成してください。」というリンクをクリック。
3. 「Apache」→「Tomcat v9.0 Server」（または同梱されているバージョン）を選択して「次へ」。
4. 「Tomcatインストール・ディレクトリー」で、Pleiadesインストールフォルダ配下の
   Tomcatフォルダを指定する（例：`C:\pleiades\tomcat\9.0.xx`）。
5. 「次へ」→ 追加ダイアログで `order-entry-system` プロジェクトを選択し、
   「追加」ボタンで右側の一覧に移動 →「完了」。
6. 「サーバー」タブに追加された `Tomcat v9.0 Server` を右クリック →「開始」。
   - コンソールにログが流れ、エラーなく起動すれば成功。

---

## 8. 画面の動作確認

1. ブラウザで以下にアクセスする。

```
http://localhost:8080/order-entry-system/
```

2. トップページから「在庫画面」「受注管理画面」に遷移し、サンプルデータが
   表示されること、CSV出力ボタンでファイルがダウンロードできることを確認する。

うまく表示されない場合は、Eclipseの「コンソール」タブに出ているエラーログを確認してください
（DB接続エラーの場合は `db.properties` の設定値、Tomcatのポート競合の場合は8080番ポートの
使用状況を確認します）。

---

## 9. マスタ管理コマンドの動作確認

1. `order-system\lib` フォルダに `ojdbc8.jar` を配置する。
   - Mavenでビルドが通っていれば、ローカルリポジトリ
     （通常 `C:\Users\ユーザー名\.m2\repository\com\oracle\database\jdbc\ojdbc8\21.9.0.0\ojdbc8-21.9.0.0.jar`）
     からコピーしてくれば良い。
2. Eclipseでプロジェクトをビルドし（自動ビルドが有効なら特に操作不要）、
   `target\classes` 配下にクラスファイルが生成されていることを確認する。
3. `order-system\bin\run_master_command.bat` をダブルクリックする。
4. コマンドプロンプトが開き、「機種マスタ管理」等のメニューが表示されれば成功。
   - 「1. 一覧表示」を選択し、サンプルデータの機種一覧が表示されることを確認する。

---

## 10. よくあるエラーと対処

| 症状 | 原因・対処 |
|---|---|
| `ORA-12154: TNS: 指定された接続識別子を解決できません` | `db.properties` の接続文字列が誤っている。`/XEPDB1` の書き方を再確認。 |
| `ORA-01017: ユーザー名/パスワードが無効です` | ユーザー名・パスワードの誤り。第3章のユーザー作成をやり直す。 |
| `ORA-28000: アカウントはロックされています` | パスワードを複数回間違えるとロックされる。SYSTEMで`ALTER USER order_app ACCOUNT UNLOCK;`を実行。 |
| Eclipseで `oracle.jdbc.OracleDriver` が見つからないと出る | `ojdbc8.jar` が依存関係・ビルドパスに追加されていない。第6章を参照。 |
| Tomcat起動時に「ポート8080は使用中です」 | 他のアプリ（他のTomcatやSkypeなど）が8080番を使用している。サーバーのプロパティでポート番号を変更するか、該当アプリを終了する。 |
| 管理コマンド実行時に `ClassNotFoundException: oracle.jdbc.OracleDriver` | `run_master_command.bat` 実行時のクラスパスに `ojdbc8.jar` が含まれていない。`lib`フォルダへの配置を確認。 |
| JSP画面で `The absolute uri: http://java.sun.com/jsp/jstl/core cannot be resolved` | JSTLライブラリが正しく取得できていない。Mavenの依存関係（`jstl:jstl:1.2`）の再取得（プロジェクト右クリック→Maven→プロジェクトの更新）を試す。 |

---

以上で、Eclipse・Oracle・Tomcatが全くない状態から、サンプルシステムの画面表示と
マスタ管理コマンドの動作確認までが完了します。
